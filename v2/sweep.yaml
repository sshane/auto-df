program: train.py
method: bayes
metric:
  name: mae
  goal: minimize

parameters:
  batch_size:
    distribution: int_uniform
    min: 8
    max: 1024
  learning_rate:
    distribution: uniform
    min: 0.0001
    max: 0.025
